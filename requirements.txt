# Core runtime needs NOTHING beyond the Python 3.10+ stdlib.
#
# Install optional features as pip "extras" (see pyproject.toml), e.g.:
#   pip install -e ".[ai-cpu]"      # local CPU GGUF model
#   pip install -e ".[ble]"         # BLE peer discovery
#   pip install -e ".[wifi]"        # raw 802.11 capture parsing
#   pip install -e ".[dev]"         # test suite
#   pip install ".[ai-cpu,ble]"     # combine as needed
#
# Pinned optional deps (kept in sync with pyproject [project.optional-dependencies]):
#   llama-cpp-python==0.3.32   # ai_backend = "cpu" (run a local GGUF model)
#                              #   aarch64/arm64 CPU wheels ship for cp38-cp312;
#                              #   Python 3.13+/other arches build from sdist
#                              #   (needs cmake + C/C++ toolchain). See docs/packaging.md.
#   bleak>=0.22                # live BLE scanning + Flippergotchi peer discovery (BlueZ on Linux)
#   scapy>=2.5.0               # native WiFi capture path
#   pytest                     # to run the test suite
#   pytest-randomly            # randomized test order in CI (catches order-dependence)
#
# Host/tools only -- NOT needed on the device runtime:
#   numpy, Pillow              # tools/gen_sprite.py (regenerate mascot sprites via Gemini)
#
# On the Flipper One itself (not needed for --simulate dev):
#   bettercap (system pkg)  # wifi recon + handshake/PMKID capture
#   gpsd      (system pkg)  # GPS fixes for the "walking" mechanic
#   RKLLM runtime           # ai_backend = "npu"  (ships with NPU driver)
